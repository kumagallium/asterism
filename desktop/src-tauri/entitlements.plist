<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
  <!-- The bundled standalone CPython loads C-extension .so files at runtime;
       disable-library-validation lets python3 load libraries not signed as
       part of its own team's static bundle. allow-jit / unsigned-executable
       memory cover extensions that JIT or map executable pages. Required for a
       hardened-runtime, notarized app that ships a Python interpreter. -->
  <key>com.apple.security.cs.allow-jit</key>
  <true/>
  <key>com.apple.security.cs.allow-unsigned-executable-memory</key>
  <true/>
  <key>com.apple.security.cs.disable-library-validation</key>
  <true/>
</dict>
</plist>
