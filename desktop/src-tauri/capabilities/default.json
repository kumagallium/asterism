{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Main window (remote loopback URL; no IPC used from the page)",
  "windows": ["main"],
  "permissions": ["core:default"]
}
