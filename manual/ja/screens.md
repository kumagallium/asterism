<!--
  Asterism マニュアル（人間向けヘルプ = AI 相談チャットの知識、単一の真実源)。
  getting-started.md の冒頭コメントに書いた表記規約（ボタンは「文言」ボタン、
  タブは「文言」タブ、サイドバーのメニュー項目はメニューの「文言」の形で書く
  こと）は本ファイルにも適用される。

  このファイルと getting-started.md だけが相談チャットのプロンプトに注入される
  （ADR design-consult-chat.md §3.8 の基本セット）。新しい機能の章を足したら、
  ここにその導線を 1〜2 行で足すこと — 詳細は各章に、場所はここに。
-->

# 画面別の導線リファレンス

かんたんモードの 7 ステップ以外で、よく聞かれる操作の場所をまとめます。
それぞれの詳しい使い方は、各節の章へのリンクを参照してください。

## 設計をやり直す・見直す

すでに公開した（または下書きの）データセットの設計を直したいときは、
データセットの詳細画面にある「設計を見直す」ボタンから、項目の意味の確認
（かんたんモードの 3. 〜 5. 相当）に戻れます。詳細は [datasets.md](./datasets.md)。

## 途中のやり直し

- かんたんモードのウィザードの中で最初からやり直したいときは、画面上部の
  手順バーにある「最初から」ボタン（① 入れる に戻る）
- 「AI が読む」の読み取りに失敗した、または「かたちをたしかめる」の下書きを
  やめて読み取りの確認からやり直したいときは「戻ってやり直す」ボタン
- 「数の確認」の内容をやめて「できた形をたしかめる」の確認に戻りたいときは
  「かたちの確認に戻る」ボタン
- 「項目の意味」から読み取りの設定に戻りたいときは「読み方の設定に戻る」
  ボタン

## 新しいデータを取り込む

メニューの「データを追加」（ホームやデータセット一覧の画面にも同じボタンが
あります）から、かんたんモードのウィザードが開きます。Excel・CSV・装置の
ファイルのほか、Word / PDF などの文書もここから取り込めます。対応形式や
読み取りの直し方は [add-data.md](./add-data.md)。

## 公開する

かんたんモードの 7. の画面、またはデータセットの詳細画面にある「公開する」
ボタンから公開できます。公開すると、このサーバを使う人が質問したときに
出典つきで引用されるようになります。

## 公開したデータを育てる（追記・差し替え）

データセットの詳細画面の「取り込み・公開」タブで、「新しい測定分を足す」
ボタン（同じ装置の新しいファイルをそのまま追加。すでにある行は二重に
なりません）と「全部を差し替える」ボタン（新しいファイルで作り直し。
差し替え中も回答は途切れません）が使えます。文書のデータセットには
「この文書を追加」ボタンで文書を足せます。詳細は [datasets.md](./datasets.md)。

## 取り込みの記録を見る

メニューの「アクティビティ」で、いつ・何が・どれだけ取り込まれたかの記録を
確かめられます。

## 使う AI（プロバイダ）を設定・変更する

左下の「設定」を開き「AI」タブで、使う AI（Anthropic / OpenAI / OpenAI 互換
など）と、そのキーを登録・変更できます。詳細は [settings.md](./settings.md)。

## このサーバへの書き込みに必要な利用許可コードを設定する

左下の「設定」を開き「このサーバ」タブの「利用許可コード」欄に、このサーバの
利用許可コードを貼り付けます。データの取り込み・公開などの書き込み操作に
必要です。

## AI の使用量・保存先・アプリの更新を確認する

左下の「設定」の「AI の使用量」タブで使ったトークンと概算コストを、
「保存先」タブでデータの保存フォルダ（デスクトップ版のみ変更可）を、
「このアプリ」タブでバージョンと更新の確認（「今すぐ確認」ボタン）が
できます。自動更新の詳細は [desktop.md](./desktop.md)。

## 公開済みデータに質問する

メニューの「質問する」から、公開済みのデータに自然な言葉で質問できます。
答えに付く出どころのたどり方は [ask.md](./ask.md)。この設計相談チャットとは
別物で、データを公開したあとに使えます。

## 公開済みデータセットの一覧を見る

メニューの「データセット」から、公開済み・下書きのデータセット一覧を見られます。
カードを開くと「中身」タブ・「取り込み・公開」タブなどの詳細画面になります
（[datasets.md](./datasets.md)）。

## データセット同士のつながりを作る・見る

メニューの「つながり」から、データセット間の共通の項目（つながり）を作った
り確認したりできます。つなぎ方の詳細は [crosswalk.md](./crosswalk.md)。
どんなデータがあってどうつながっているかを 1 枚の図で見るには
「全体像を見る」ボタンです。

## 使われている用語（共通の語彙）を見る

メニューの「共通の言葉」から、公開済みデータで使われている用語（クラス・
項目名）の一覧を見られます。用語を世界の標準に合わせるには、データセットの
「中身」タブで候補から選び「この標準に合わせる」ボタンです
（[vocab-and-grounding.md](./vocab-and-grounding.md)）。

## 設計中の疑問をその場で聞く（相談チャット）

どの画面でも右下の「AI に相談」ボタンで、この相談チャットが開きます。
Cmd+Enter で送信、履歴・検索・書き直しの使い方は [consult.md](./consult.md)。

疑問が生まれやすい場所には、文面の入った状態で開くボタンを置いています。

- 「項目の意味」の画面 — 意味が空欄のとき「AI に相談して埋める」ボタン
- 「外とのつながり」の画面 — 「迷ったら AI に相談」ボタン
- 「できた形をたしかめる」の画面 — 「つながりを AI に相談」ボタン /
  「分け方を AI に相談」ボタン
- 同じ ID で同じ項目を持つ種類が 2 つあるとき — 「重複を AI に相談」ボタン

相談チャットが具体的な候補を挙げたときは、返事の下に候補の一覧と
「この候補を表に反映」ボタンが出ます。何が起きるかを読んでから押してください
（反映するかどうかは、いつでもあなたが決めます）。

## かんたんモードと詳細モードの切り替え

ウィザードのどの画面でも「詳細モードへ」ボタンで、変換ルールや検査結果まで
すべての項目を表示する詳細モードに切り替えられます。かんたんな画面に戻る
ときは「かんたんモードへ戻る」ボタンです。

## 関連

かんたんモード 7 ステップの流れそのものは
[getting-started.md](./getting-started.md) を、マニュアル全体の目次は
[index.md](./index.md) を参照してください。
